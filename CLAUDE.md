# CinemaTracker — Codebase Guide

A single-page web app for tracking movies watched, ratings, lists, social feed,
data dashboards, AI recommendations, achievements, and custom themes. Vanilla
JS + HTML + CSS on the front end; **Supabase** (Postgres + Auth + Storage + Edge
Functions) on the back end. No build step, no framework, no bundler.

## 📌 KEEP THIS FILE CURRENT (do this every time)

**Whenever you add/remove/rename/move a file, feature, page, or notable function,
update this `CLAUDE.md` in the same change** so the file map, function list, and
script-load order below stay accurate. Treat updating this guide as part of the
task, not an optional extra. Specifically:
- New `assets/js/*.js` file → add it to the **File structure**, the **JS file map**
  table, and add its `<script src>` tag to `index.html` in the correct order.
- New/removed/renamed function → update the affected row in the JS file map.
- New page/route → note it under `02-router.js`.
- New back-end dependency (edge function / SQL / RPC) → update the **Back end**
  section.
After structural edits, you can regenerate the function inventory with:
`grep -rnE "^        (async function|function|class) " assets/js/`

## ⚠️ Critical constraints — read before editing JS

The front end was refactored from one giant `index.html` into many files, but it
is **NOT modular**. All the `assets/js/*.js` files are loaded as ordinary
(classic) `<script>` tags, in numeric order, at the end of `<body>` in
`index.html`. They all share **one single global scope**. This has hard rules:

1. **Load order matters.** The `<script src=...>` tags in `index.html` are
   ordered `01` → `18`, then `20-leaderboard.js`, `21-swipe-deck.js`,
   `22-account-fun.js`, then `19-logging-boot.js`.
   `01-config.js` defines globals (`supabaseClient`, `icons`, `COL_WATCH_DATE`)
   that everything else depends on; `19-logging-boot.js` runs the boot sequence
   (`DOMContentLoaded`, etc.) and **must stay last** — so the higher-numbered
   `20-leaderboard.js` / `21-swipe-deck.js` / `22-account-fun.js` are intentionally
   loaded just *before* it.
2. **No `import`/`export`, no `type="module"`.** Adding modules would break the
   shared-scope model and every inline `onclick="..."` handler.
3. **Functions are global by design.** The HTML markup in `index.html` calls them
   inline (e.g. `onclick="router.navigate('home')"`, `onclick="openAuthModal()"`).
   A function defined in any file is callable from any other file and from inline
   handlers. Do not wrap files in IIFEs or change a top-level `function`/`const`
   to something scoped.
4. **A function can live in a different file than where it's used.** Files were
   split by original source order, not perfectly by domain. If you can't find a
   function, grep all of `assets/js/`:
   `grep -rn "functionName" assets/js/`
5. **Don't assume a file is self-contained.** Cross-file calls are everywhere
   (e.g. dashboard code calls theme/auth helpers).

If you ever need to recombine or re-split, the untouched pre-refactor originals
are in `_original_backup/` (`index.html.bak`, `reset-password.html.bak`).

## File structure

```
index.html                     HTML markup only; <link> to CSS + ordered <script src> tags.
                               Includes the top `.navbar` (desktop nav + the now
                               mobile-hidden hamburger/`#mobile-menu`) AND the mobile-only
                               `#mobile-tabbar` bottom tab bar (Home/Feed/My Movies/Lists/More — 5 tabs)
                               + the `#more-sheet-overlay` "More" bottom sheet (a 2-up
                               icon-tile grid `.more-sheet-grid` — Discover, AI Picks,
                               Data Dash, Leaderboard + a full-width auth row
                               `.more-sheet-item-auth` `#more-auth-btn` that doubles as
                               Account when logged in (so there's no separate Account tile);
                               each tile is `.more-sheet-item` = `.more-sheet-ico`
                               brand-tinted icon + `.more-sheet-label`).
                               Tab bar + sheet are
                               styled mobile-only (≤768px) in styles.css. On mobile the top
                               nav bar keeps the compact **logo** on the left (the
                               "CinemaTracker" wordmark `.nav-brand-text` is hidden) followed by
                               `#mobile-page-title` — a dynamic header showing the current page
                               name (set in `router.navigate` from a page→title map); the
                               account/avatar button stays on the right for all pages — its avatar
                               (rendered at 60px) is forced to fill the 34px icon button so it
                               doesn't overflow. Mobile header sizing is unified across ALL
                               widths (incl. ≤520 + Home): `--header-height` 64px, logo 46px,
                               avatar button 34px. Also holds `#ptr-indicator`, the
                               pull-to-refresh spinner.
reset-password.html            Standalone password-reset page (Supabase recovery flow)
assets/css/styles.css          All app styles (~3,400 lines)
assets/css/reset-password.css  Styles for reset-password page
assets/js/01..18-*.js          App logic (see map below)
assets/js/20-leaderboard.js    Leaderboard sub-tab logic (loaded BEFORE 19; see map)
assets/js/21-swipe-deck.js     Discover swipe deck (loaded BEFORE 19; see map)
assets/js/22-account-fun.js    Fun Account page (route `account`): big avatar, AI taste
                               blurb, bio editor, follow counts, taste card (loaded BEFORE 19)
assets/js/19-logging-boot.js   Boot sequence — loaded LAST (see map)
assets/js/reset-password.js    Logic for reset-password page
manifest.webmanifest           PWA manifest (installable web app; name/icons/colors)
service-worker.js              PWA service worker — push notifications + NETWORK-FIRST
                               fetch (always loads latest deploy, cache only as offline
                               fallback) + on-activate it messages clients to reload onto
                               the new version (installed PWA auto-updates, no reinstall).
                               Bump CACHE_VERSION to force. Registered in 19-logging-boot.js.
                               `push` handler sets the iOS app-icon badge from the payload's
                               numeric `badge`; `notificationclick` focuses an open client +
                               posts `NOTIFICATION_NAV` (or opens at the #hash) so the app
                               routes to Feed/Recs and clears the badge.
assets/icons/                  PWA icons (icon.svg source + generated PNGs:
                               icon-192/512, icon-maskable-512, apple-touch-icon 180)
_original_backup/              Byte-identical pre-refactor originals (safety net)
.github/workflows/refresh-imdb.yml  Daily GitHub Actions cron → calls the swift-api
                               `refresh_imdb_ratings` Edge action (keeps IMDb ratings fresh)
.github/workflows/refresh-taste-profiles.yml  Daily GitHub Actions cron → calls the
                               swift-responder taste edge in CRON_SECRET batch mode
                               (recomputes stalest taste profiles + one-time backfill)
.github/workflows/backfill-movie-keywords.yml  Manual (workflow_dispatch) GitHub Actions
                               job → loops the swift-api `backfill_movie_keywords` action
                               until the whole catalog has TMDB keywords (one-click catch-up)
```

PWA install (iPhone): `index.html` `<head>` has the `apple-mobile-web-app-*` meta
tags + `apple-touch-icon` + `<link rel="manifest">`, so Safari → Share → Add to
Home Screen launches it full-screen. Push notifications need this install (iOS
16.4+). Real remote Web Push IS built (VAPID keys + `push_subscriptions` table +
the swift-api `notify_*`/`send_recommendation`/`test_push` edge actions). The
"Send test" button on the Account page (`enableNotificationsTest` in
`18-account-page.js`) now does a **real end-to-end test**: it calls the `test_push`
edge action (which web-pushes the caller's own subscribed devices), and only if
that delivers nothing (push not enabled here, or VAPID secrets unset) falls back to
a LOCAL `showNotification` + reports why the real push didn't go out.

The refactor was verified byte-for-byte: concatenating the split files
reproduces the originals exactly. No logic was changed.

## JS file map (where to look for what)

| File | Domain |
|------|--------|
| `01-config.js` | Supabase client init (`SUPABASE_URL`/`SUPABASE_KEY`), `icons` SVG map (incl. `filter`/`sort`/`clearX`/`users` used by the icon-only Filter/Sort/Clear/Follows control buttons), `COL_WATCH_DATE`, nav logo loading |
| `02-router.js` | The `router` object — **all page view HTML templates** (home, feed, library, lists, **discover**, ai_picks, dashboard, **account**, **settings**, **leaderboard**) live here as template strings. **Account is split into two routes:** `renderAccount` (route key `account`, the avatar/Account nav button + mobile "More" auth row land here) is the NEW **fun profile page** — its shell + the bio-edit modal `#account-bio-overlay`; its logic lives in `22-account-fun.js` (`initAccountHome`/`loadAccountHome`). A ⚙ **Settings** button on it (`data-account-home-action="open_settings"`) navigates to `renderSettings` (route key `settings`) — the OLD account card-list (Profile / Notifications / Security / Leaderboard / Feature Requests / Theme / Log out + the admin panel + all the section modals), still driven by `initAccountPage`/`loadAccountPage` in `18-account-page.js`. The **Discover** route (`renderDiscover`, route key `discover`, reached from the mobile "More" sheet + desktop nav link) is the swipe deck shell (`#discover-stack` + Skip/Details/Like action row) driven by `21-swipe-deck.js` (`initDiscoverPage`). Largest file; one big object literal. The **Leaderboard** view is its own gated route (`renderLeaderboard`, route key `leaderboard`, reached from the Account page's "Leaderboard" card / `open_achievements` action + the mobile "More" sheet) — a two-sub-tab shell (`.lb-subtabs`): **Leaderboard** (`#lb-panel-leaderboard`, the friends-ranking view driven by `20-leaderboard.js`) and **Achievements** (`#lb-panel-achievements`, which wraps the SAME achievements markup/DOM ids as before — tier summary + badge grid + test panel — still loaded by `loadAchievementsPage` in `18-account-page.js`). **Each panel carries its OWN controls just below the sub-tabs** (not in the page header) so the two sub-tab buttons stay on the same Y on both sub-tabs; `setLeaderboardSubtab` (in `20`) just shows/hides whole panels. The Achievements panel's controls live in a `.lb-ach-toolbar`: a **This Month / All-Time** timeframe pill group (`#account-achievement-timeframe-wrap`, `[data-ach-timeframe]` → `setAchievementTimeframe` in `11`) + the **Filter** (`#account-achievement-filter-btn`) / **Sort** (`#account-achievement-sort-btn`) buttons sharing ONE popover (`#account-achievement-filters-pop`, `setAchievementFiltersOpen(isOpen, mode)` shows only the `[data-af="sort"]`/`[data-af="filter"]` row). On mobile the redundant `.page-title-card` is hidden (the header bar already says "Leaderboard"). (This route was renamed from `achievements`; the dispatch in `navigate()` now keys on `page === 'leaderboard'` and also calls `initLeaderboardPage()` + `setLeaderboardSubtab('leaderboard')`.) |
| `03-home-dashboard-core.js` | `loadDashboard`, dashboard data helpers (`dash*` formatting/poster/person), feed/library/lists auth warnings |
| `04-lists.js` | Lists feature: state, create/rename/delete modals, sort+filter modal, add-to-list, bucket list, `initListsPage`, `loadListsPage`. **Perf:** `loadListsPage` reads the **`user_list_items_v1` SQL view** (`LIST_ITEMS_VIEW`, see `lists_views.sql`) in ONE query — all movie metadata (genre/IMDb/director/runtime/MPA/actors + the user's own rating + watch info) is pre-joined in the DB, replacing the old per-movie live TMDB calls that made this page slow / sometimes never load. Recs recommender avatars + watch platforms remain separate single batched queries. **Movie Recommendations** modal (`openRecModal`/`openRecModalFromHome`/`sendRecommendation`): pick people you follow → `send_recommendation` Edge action adds the movie to each recipient's auto "Recs" list + sends a web push. Recs cards show recommender avatar(s) + a "+" that opens `openRecByModal` (all recommenders); send blocks recipients who already saw the movie or were already recommended it. In the modal, followers who have **already seen** the movie are greyed out + un-checkable with an "Already seen this" note and a **"View review"** button (`openRecReviewModal`) that shows their rating/review in the `#rec-review-overlay` modal (seen-status computed in `loadRecRecipients` by reading their `Movie Ratings` for the resolved movie id). Followers you've **already recommended** this movie to are likewise greyed out + un-checkable ("Already recommended", `recAlreadyRecByUserId` from reading your own `Recommendations` rows). The recommendation log is the source of truth for "pending rec": when a recipient **removes** the movie from their Recs list without watching it, `clearReceivedRecommendations` deletes the `Recommendations` rows (needs the recipient-DELETE RLS policy in `recommendations_tracking.sql`) so a sender can recommend it again; watched movies stay blocked by the seen-check. New recs glow (`.is-new`) until the Recs list is viewed. **"Recs" is the default list shown when opening the Lists tab** (falls back to Bucket List / first list). When the active list is **Recs**, the manual "Add movies" search bar (`.lists-add-search`, hidden in `setListsQuickAddEnabledState`) and the **Watch Count Range** filter (`#lists-watch-count-range`, hidden in `configureListsSortFilterModalForActiveList`) are hidden (Recs is auto-managed + unwatched). List posters **hover-flip** to a details back face (Director/Runtime/MPA/Genre/IMDb) reusing the My Movies `.library-poster-flip*` classes. On the **Recs** list, tapping a poster opens the **Recs viewer** (`openRecsMovieModal` → `#recs-movie-overlay`) instead of the log form: if anyone you know (the recommender(s) + people you follow) has rated the movie it shows a choice screen — **Movie Details** (genre/MPA/runtime/year/director/IMDb), **User Reviews** (their feed-style review, one reviewer = straight to it), and/or **Watch Options** (`recsMovieRenderWatchOptions` — the streaming platforms, from `recsViewPlatforms`/`listsPlatformsByMovieId`); the choice screen now appears when there are reviewers OR platforms, else it goes straight to Movie Details. Reviewer rating rows are read from `Movie Ratings` for the candidate user ids. The old per-card **"Watch Options" button was removed** (its platforms list moved into this viewer), as were the per-card IMDb/runtime subtext lines (shown under Movie Details instead); the recommender avatar on each card is slightly larger. (The standalone `openListsWatchOptionsModal`/`#lists-watch-options-overlay` are now unused/dead.) Logging/rating a movie auto-removes it from the user's **Bucket List + Recs** via `removeMovieFromAutoLists` (called from the diary save). **Lists overview (Spotify-style):** the Lists page now opens to a **cover grid of all lists** (`#lists-overview` / `loadListsOverview`, 3-up on mobile, auto-fill on desktop) instead of a text dropdown; each card's art (`renderListCoverArt`) is: the list's saved `cover` image (an inline data URL — including the **branded covers the DB stores for the auto-managed Recs / Bucket List**, see `lists_branded_covers.sql`; this replaced the old front-end `specialListCoverUrl` that forced large local PNGs and lagged behind normal covers), else a 2×2/strip **collage of its movie posters**, else a colored fallback tile (`icons.star` for Recs, `icons.film` otherwise). The Recs / Bucket List covers are still **non-editable** (the Edit modal hides cover upload + "Remove photo" for them via `isSpecialAutoList`). Tapping a card → `openListFromOverview(id)` switches to the **detail view** (`#lists-detail` — the existing per-list movie layout, now with an `← All Lists` back button → `showListsOverview`). The overview orders **Recs first, then Bucket List, then the rest**. The **Recs cover shows an unseen-recs badge** in its top-right corner (`#lists-cover-badge-recs`, `.lists-cover-badge`) — `setNavBadge('nav-badge-lists', …)` in `05-feed-library.js` updates it as another twin of the nav/tab-bar Lists badge, and `loadListsOverview` calls `refreshNavBadges()` after render to reconcile it. `enterListsPage` (called from `router.navigate`) shows the overview by default but opens a list directly on deep-link (`listsPendingSelectName`, e.g. "Recs" from a push). Per-list **cover upload** (the old per-card pencil overlay was removed — covers are now changed only from inside a list via the Edit modal): `triggerListCoverPick` → shared hidden `#lists-cover-input` → `handleListCoverPick` center-crops/downscales the photo to a ~320px JPEG **data URL** via the shared `processAccountIconFile` and saves it to `Lists.cover` (needs `lists_cover_column.sql`). The old `#lists-select` dropdown is kept hidden (still drives `loadListsPage`). `listsViewMode` ('overview'|'detail') gates which panel shows. On mobile the dynamic header bar reads **"Lists"** on the overview and **"Lists - <list name>"** inside a list (`setListsMobileHeader`, called from `showListsOverview`/`openListFromOverview`/`loadListsPage`). **New List / Add Movie** — the action is the same everywhere (Create List on the overview = `openListsCreateModal`; **Add-Movie modal** inside a list = `#lists-add-overlay` / `openListsAddModal`, a Home-style search `#lists-movie-search-input` → `handleListsAddMovieSearch` with **Year/MPA filters** `applyListsAddFilters` → `listsAddAppliedYear/Mpa` → `callSwiftApiSearchMoviesForLists`; picking a result adds it to the active list), but the **trigger differs by viewport**: **MOBILE** uses a single contextual **`+` FAB** (`#lists-fab` → `listsFabAction`); **DESKTOP** uses clearly-labelled inline buttons — **"New List"** (`#lists-new-list-btn`, in the page header row) on the overview and **"Add Movie"** (`#lists-add-movie-btn`, in the detail toprow) inside a list. Both inline buttons carry the `.lists-desktop-action` class (CSS hides them ≤768px); the FAB is hidden ≥769px (CSS) — so exactly one control shows per viewport. `updateListsFab()` drives all three: it hides the FAB **and** the "Add Movie" button inside the auto-managed **Recs** list (no manual adds), shows "New List" only on the overview, and "Add Movie" only inside a non-Recs list. The FAB lives at **body level in `index.html`** (NOT inside `#app-root`, because the page wrapper `.fade-in` keeps a `transform` via `animation-fill-mode: forwards`, which would make a `fixed` child anchor to the page box instead of the viewport — that bug put it mid-screen). It's fixed bottom-right (above content + the tab bar, z-index 120), shown only on the Lists page via `setListsFabVisible()` + a `body:not([data-page="lists"]) #lists-fab { display:none }` gate. **Refresh** was removed in favor of **pull-to-refresh** (the Lists overview opts into `pullToRefreshAction`/`pageSupportsPullToRefresh` in `09-home-ui.js`). The **detail** view's top is a single row: **← All Lists** (back) + **Filter** + **Sort** + **Edit** + (desktop) **Add Movie** (heading sits below). Filter+Sort open the shared `#lists-sortfilter-overlay` showing only the `[data-sf=filters]` or `[data-sf=sort]` section (`openListsSortFilterModal(mode)`, mirrors My Movies); **Edit** (`openListsEditModal`) opens the combined edit modal (the old "Rename List" overlay, now "Edit List") = change/remove **cover photo** (a large centered preview with a small camera upload button `#lists-edit-cover-upload`; the upload button + "Remove photo" are hidden for **Recs**/**Bucket List** since they use the fixed branded cover) + **rename** + **delete** (the latter two hidden for the auto-managed **Recs**/**Bucket List** via `isSpecialAutoList`, which also can't be renamed/deleted). The modal has **one Save button** (`submitListsEditModal` → saves the name via the rename form if changed, else just closes; cover changes persist immediately on pick). Delete still routes through the existing `#lists-delete-overlay` confirm (`openListsDeleteFromEdit`). **SHARED (group) lists** (`shared_lists.sql`): a list can be co-owned by the creator + people they follow. **Membership** lives in the `List Members` table; a user is a "member" if they are `Lists.user_id` (owner) OR have a `List Members` row. **"Any member" admin** — every member can add/remove movies, rename, delete, and add/remove other members. Each list owns a tag set by `loadUserLists` (`owner_id`/`shared`/`is_owner`); `loadUserLists` now merges in lists the user is a member of (queries `List Members` → those `Lists` rows). Helpers `isSharedList(id)`/`listOwnerId(id, fallback)`/`getCachedListRow(id)` resolve which `user_id` keys a list's rows: **every `Movie Lists` row for a shared list carries the OWNER's `user_id`** (so the owner-keyed `user_list_items_v1` view + reads resolve), plus an `added_by` column recording who actually added the movie. So `addMovieToList`/`removeMovieFromList` and the cover/rename/delete `Lists` mutations all key on `listOwnerId(...)` (RLS `*_member` policies let any member write); `addMovieToList` also fires the **`notify_list_add`** edge action fire-and-forget when the list is shared, so the OTHER members get a push that the movie was added (deep-links to the list via `#list=<id>` → `listsPendingSelectId`); `loadListsPage` queries the view with `viewUserId = listOwnerId` for shared lists (and **clears the per-card rating overlay** since those rows carry the owner's rating, not the viewer's); `loadListsOverview` queries `Movie Lists` by `list_id IN (...)` (not `user_id`) so members see shared-list collages, and shared cards show a **people-icon chip** (`.lists-cover-shared`, `icons.users`). **Create** (`#lists-create-overlay`): a **"Shared list" checkbox** (`#lists-create-shared`, `toggleListsCreateShared`) reveals a member picker of people you follow (`loadListsFollows`/`renderListsCreateMembers`/`toggleListsCreateMember`, `listsCreateSelectedMemberIds`); on save `createUserList({is_shared})` + `addListMembers(...)`. **Edit modal** shows a **Members section** for shared lists (`loadListsEditMembers`/`renderListsEditMembers`/`removeListMember` + an "Add member" picker `toggleListsEditAddMode`/`renderListsEditAddPicker`/`addListMemberFromPicker`; owner row is non-removable). **Tapping a movie on a shared list** opens `openSharedListMovieModal` (`#shared-movie-overlay`): lists the members who have a review of that movie (read from `LIBRARY_ITEMS_VIEW` for member ids, only rows where `overall_rating` exists — a member with no review isn't listed; the viewer's own review shows labeled "You"), each row → `openProfileMovieReview(memberId, movieId)` to read their full diary entry. |
| `05-feed-library.js` | **Top control rows (My Movies + Feed):** the **Filter** + **Sort** + **Clear** buttons are icon-only (`.controls-icon-btn`, `icons.filter`/`icons.sort`/`icons.clearX`), and Feed's **Follows** button is icon-only too (`icons.users`); the My Movies view-toggle reads just **"Grid"/"List"** (the view you'll switch to, set in `syncLibraryViewUI`). Each page's **Clear** button (`data-library-action="clear"` / `data-feed-action="clear"`) resets ALL active filters/sort + the title search to default and reloads — My Movies clears `librarySortFilterState`→default + `librarySearchQuery`; Feed clears `feedExcludedUserIds`/`feedCompareOwn`/`feedInCommonOnly` + `feedSearchQuery`. (`syncFeedFilterButton` no longer rewrites the button's `textContent` — that would wipe the icon — it only toggles `.filter-active`.) **Title search (My Movies + Feed):** a magnifier button (`#library-search-btn` / `#feed-search-btn`, `.page-search-btn`, `open_search` action) on each page's control row opens the shared **`#page-search-overlay`** popup (`openPageSearch(context)`/`closePageSearch`/`submitPageSearch`/`clearPageSearch`, state `librarySearchQuery`/`feedSearchQuery`/`pageSearchContext`); submitting filters the page to movies whose title close-matches the query — **loose/"fuzzy"** so "Du" finds "Dune" and word order doesn't matter (`normalizeSearchText` strips accents/punctuation + lowercases; `movieTitleMatchesSearch` requires each query word as a substring). **My Movies** filters **server-side** (per-word `ilike('title', …)` added in `libraryBuildServerQuery`, paginated as usual); **Feed** filters the already-loaded rows **client-side** in `loadFeedItems` before grouping. The button shows the vibrant `.filter-active` highlight while a search is set (`syncPageSearchButton`, re-applied on each page load); on mobile it stays a compact icon-only magnifier (label hidden, opts out of the equal-width row). "My Movies" library (render/filter/sort/paginate, Edit/Delete/Recommend buttons; **tapping a grid poster opens `openLibraryMovieModal` — full diary entry: ratings, sub-ratings, watch info, quote, notes + Edit/Delete/Recommend — via the `#library-movie-overlay` modal**; `openLibraryMovieModal` self-fetches the row from `LIBRARY_ITEMS_VIEW` if it's not in the `libraryItems` cache, so it also works when opened from the **Data Dashboard**) AND social Feed (following, user search — by **username OR email** via the `search_users` RPC (`search_users.sql`), with a username-only client-query fallback; feed items grouped by movie and ordered by **`Movie Ratings.created_at` desc** — most-recently **POSTED** reviews first (original post time, NOT watch date and NOT `updated_at`), so going back to **edit** an old rating updates it in place without resurfacing it to the top of followers' feeds (`movie_ratings_created_at.sql` adds + backfills the column) — in the normal feed via `loadFeedItems` (the "in common" mode still uses Watch Logs); Filter modal: per-follow checkboxes + Compare Own + **"Only show movies in common"** = keep only movies watched by 2+ of the shown users, `feedInCommonOnly`; this mode loads watch logs 1000 at a time with a **"Load More"** button (`renderFeedInCommonLoadMore`, `loadFeedItems({appendInCommon:true})`) so older overlaps aren't cut off). Also the **nav notification badges** (`refreshNavBadges`/`markFeedSeen`/`markRecsSeen`): red count on the Feed nav button = new follow ratings since last view, on the Lists button = new recs received; last-seen times are **cross-device**: the authoritative value lives in `Users.feed_seen_at`/`recs_seen_at` (read via `loadSeenTimesFromDb`), with `localStorage` only a per-device cache; `effectiveSeen` uses the LATER of the two so a badge cleared on one device reads cleared on another (badges also re-check on tab focus/`visibilitychange`). Brand-NEW feed cards from **other** users (by `created_at` since last seen — an edit of an old review does NOT glow or re-count) glow in the **theme's primary color** (`.is-new`, `var(--brand)`) — never your own; a NEW entry for a movie **you recommended to that person** glows in the **theme's secondary accent** (`.is-new-rec`, `var(--brand-2)`, distinct from `.is-new`, via `myRecSentPairs` from the `Recommendations` table). Also the **user profile overview modal** (`openUserProfile`): click a user in the Feed → KPIs (movies watched, avg overall, highest-rated director) + a **Recent / Top Rated** poster grid (the **10** most recent + top **10**), computed client-side and rendered with the Data Dash helpers (`dashRenderHelpScore`/`dashRenderHelpTier`/`dashBuildPosterUrl`/`.dash-fav-grid`) for an exact visual match. **Every poster in the grid is clickable** → `openProfileMovieReview(profileViewUserId, movie_id)` opens that user's full diary entry in the same `#library-movie-overlay` modal as the Biggest-disagreements chips. The **Top Rated** toggle reveals a metric dropdown (`#profile-top-metric`, `setProfileTopMetric`, options in `PROFILE_TOP_METRICS`: Overall / Acting / Cinematography / Plot / Pacing / Dialogue / Sound) that re-sorts the top 10 by the chosen `Movie Ratings` sub-rating; the sort/slice happens in `renderProfileGrid` off `profileRatedItems` (all rated movies, incl. sub-ratings) + `profileRecent10`. When viewing **someone else's** profile it also shows a **Taste Match** card: a compatibility % based on the **Pearson correlation** of your overall ratings across movies you've both rated (mapped `(r+1)/2 → 0–100%`, so it measures how similarly you RANK films, immune to harsh/generous-rater offset — a random pair ≈ 50%), with **confidence shrinkage toward 50%** by shared-movie count (`(n·raw + 4·50)/(n+4)`) and a fallback to the simple `100 − avg gap` closeness when there are <3 shared movies or no rating spread to correlate + count in common + the 3 biggest disagreements (computed in `openUserProfile`, rendered via `renderProfileBody`'s `compat`). The **Biggest disagreements** section has its OWN **metric dropdown** (`#profile-disagree-metric`, `setProfileDisagreementMetric`, same `PROFILE_TOP_METRICS` options: Overall / Acting / Cinematography / Plot / Pacing / Dialogue / Sound) so you can rank the disagreements by ANY sub-rating, not just Overall — `compat.pairs` carries BOTH users' full rating rows (`mineRow`/`theirsRow`, cached in `profileCompat`) and `renderProfileDisagreements()` filters to movies where both rated the chosen metric, sorts by that metric's gap, takes the top 3, and re-renders into `#profile-disagree-list` on change (no reload). Each disagreement row's title is formatted **"Title (Year) - MPA"** (year/MPA from the `Movies` row, omitted if missing; `mpa_rating` is included in the profile's `Movies` query and carried on `compat.pairs`). In **Biggest disagreements** each row shows two clickable chips: **You — N <Metric>** (the score is colored by YOUR tier for that movie, tap → `openLibraryMovieModal`) and **@them — N <Metric>** (score colored by THEIR tier, tap → `openProfileMovieReview(userId, movieId)` which shows that user's full diary entry for the movie in the **exact same Diary Entry modal** (`#library-movie-overlay`) as your own — both `openLibraryMovieModal` and `openProfileMovieReview` build the body via the shared `renderLibraryDiaryBody(it, {showActions})` helper off a flattened `LIBRARY_ITEMS_VIEW` row (`openProfileMovieReview` reads the other user's view row by `user_id`+`movie_id`, sets the header to "Name's Review", passes `showActions:false`, and bumps the overlay z-index above the profile modal); the "N% apart" label is forced white. The profile poster cards title-line is **"Title (Year)"** with the score/tier on the line below. |
| `06-dashboard-controls.js` | Data Dash UI: KPI clicks, tab/timeframe/metric controls, chart control wiring, `setDashboardTab`, favorites. Clicking any movie poster (`[data-dash-movie-id]`) opens the **My Movies diary popup** (`openLibraryMovieModal`, with `initLibraryPage()` first so its Edit/Delete/Recommend handlers are bound) — NOT the Update Ratings form. **Favorites tab posters** show a circular rank badge (`.dash-fav-rank`, styled in styles.css) in the top-left corner — `#1`, `#2`, … (`idx + 1` within each Top/Bottom row) — and the Favorites grid is **3-up on mobile** (base `.dash-fav-grid` = 3 cols). `setDashboardTab` also calls `dashCenterActivePill()` (defined in `09`) to keep the active pill centered in the **mobile sticky tab indicator** (see the mobile Data Dash overhaul note on `09`). |
| `07-dashboard-charts.js` | Data Dash rendering: `loadDashboardCharts/Ratings/General/Tiers/QuoteWall`, pie/bar/donut chart drawing |
| `08-search-trending.js` | TMDB-backed movie search + trending (`callSwiftApi*`), home search results, lists quick-add search |
| `09-home-ui.js` | Home page UI helpers, search filters panel, duplicate-rating modal, `handleSearch`, `toggleMobileMenu`, `escapeHtml`. Also the **mobile bottom-tab-bar "More" sheet** (`openMoreSheet`/`closeMoreSheet` toggle `#more-sheet-overlay.open`). The tab bar's **active state is pure CSS** (`body[data-page=...]` selectors in styles.css — `router.navigate` already sets `document.body.dataset.page`, so no JS wiring). Feed/Lists tab badges reuse `setNavBadge` in `05-feed-library.js`, which now mirrors counts to the `-t` (tab-bar) twin ids alongside the existing `-m` (mobile-menu) twins. The "More" auth row (`#more-auth-btn`) label is kept in sync wherever `#mobile-auth-btn` is (`14-themes.js` `refreshAuthStateAndUI`, `13-auth-guest.js` enter/exit guest) — these write the button's `.more-sheet-label` span (not the button's `textContent`, which would wipe the tile's `.more-sheet-ico` icon). The 4 primary tabs call **`tabNav(page)`** (not `router.navigate` directly): light haptic (`navigator.vibrate`, no-op on iOS) + tapping the tab you're already on smooth-scrolls to top instead of re-rendering. **Native-feel helpers** (all here, all mobile-gated via `isMobileViewport()`): `animatePageEnter(root)` — Phase 2 page transition, called by `router.navigate` right after it grabs `#app-root`, toggles the `.page-enter` class (CSS `@keyframes pageEnter` fade+slide, removed on `animationend` so no transform lingers to trap fixed overlays; respects `prefers-reduced-motion`); `skeletonRows`/`skeletonPosters`/`loadingPlaceholder(kind)` — Phase 4 loading skeletons (`'rows'` = library/feed stacked cards, `'posters'` = lists poster grid) injected in place of the old "Loading…" text on **BOTH mobile and desktop** (the mobile-only gate was removed — `loadingPlaceholder` now always returns a skeleton) by `loadLibraryPage`/`loadFeedItems` (`05`) + `loadListsPage` (`04`). **Infinite scroll** (`attachInfiniteScroll(sentinel, onReach)`/`detachInfiniteScroll`, also here): one shared `IntersectionObserver` (only one infinitely-scrolling list is on screen at a time) — a page renders a bottom **sentinel** (`.infinite-sentinel`) and the observer auto-loads the next page when it nears the viewport (rootMargin prefetch). Used by **My Movies** (`loadLibraryMore`, page size `libraryLimit`=100, observes the `#library-load-more-wrap` — its manual "Load More" button stays as a fallback), **Feed** (`loadFeedItems({appendNormal:true})` via `renderFeedNormalLoadMore`'s `#feed-load-sentinel`, page size `FEED_NORMAL_PAGE`=100, accumulated in `feedNormalRows`), and **Lists detail** (`renderListsMoreCards` streams already-built card HTML from `listsPendingCardsHtml` in `LISTS_RENDER_PAGE`=100 chunks via `#lists-load-sentinel` — pure client-side windowing, no extra DB calls). **Mobile CSS (Phases 1/2/4):** styles.css also has the global "kill browser tells" block (`overscroll-behavior:none`, no tap-highlight, no iOS callout on chrome/posters), the **full-bleed mobile container** rules (≤768px, scoped to `#app-root`: zero `.container` side padding, slim `.glass-panel` horizontal padding, direct-child `.glass-panel > .glass-panel` flatten so there's no container-in-container), the `.page-enter` transition, `:active` press-feedback scale on tappables, and the `.skel*` shimmer skeleton styles. **Phase 3 — bottom-sheet modals:** because the whole app shares the `.auth-overlay > .auth-modal` modal pattern, ONE gated CSS block (keyed on `body.app-sheets`, set on the `<body>` tag in index.html) docks every modal to the bottom of the screen on mobile as a slide-up sheet with a grab handle (CSS `@keyframes sheetUp`, not a transition, so it animates even though modals open via inline `display:flex`). Two celebration popups (`#achievement-earned-overlay`/`#achievement-detail-overlay`) **and the login/auth modal `#auth-overlay`** are excluded (the three sheet selectors carry `:not(#auth-overlay)`). The auth modal instead uses the compact **top-anchored** mobile styling (`align-items:flex-start`, content-height) so it's smaller and — critically — its top stays reachable when the on-screen keyboard pushes content up (a bottom-docked sheet had its top scrolled off-screen). **To revert bottom sheets, remove the `app-sheets` class from `<body>`** — no other change needed. **Swipe-to-dismiss** (`initSheetSwipeDismiss` IIFE in `09-home-ui.js`, mobile + `app-sheets` only): dragging a sheet down (only when it's scrolled to its top, so internal scroll wins first) translates it with the finger; releasing past ~110px animates it out and closes it (`dismissSheet`), otherwise it snaps back. `findSheet` covers the shared `.auth-overlay > .auth-modal` data modals, the bottom-tab **"More" nav sheet** (`.more-sheet-overlay > .more-sheet`, whose visibility is the `.open` class, not `display`), **and bespoke `.open`-toggled bottom sheets that aren't an `*-overlay` id** — currently the Feed **"Follows" panel** (`.feed-follows-panel`, opened by `openFeedFollows`). Bespoke sheets return a `close` callback from `findSheet` so `dismissSheet` routes through their own teardown (`closeFeedFollows` re-homes the moved DOM node + hides its backdrop) instead of `ov.click()`. **`initPullToRefresh`'s `anyModalOpen()` must also know about these custom sheets** (it now checks `.feed-follows-panel.open, .more-sheet-overlay.open` on top of the `*-overlay` scan) — otherwise a downward drag on the open Follows panel pull-to-refreshes the feed BEHIND it. `dismissSheet` closes via the overlay's own backdrop-close handler (`ov.click()` — every overlay closes on a backdrop click) plus a force-hide fallback, but **never inline-hides the More sheet** (it toggles a class; an inline `display:none` would break re-open). **The touchmove listener is non-passive and calls `preventDefault()` while actively dragging a sheet down, so the page/data BEHIND the modal does NOT scroll along with the drag.** **Swipe-to-dismiss now covers EVERY pop-up modal** — `EXCLUDE` skips ONLY `loading-overlay` (a blocking spinner with no close handler that must not be swiped away). The auth/login modal (`#auth-overlay`) and the two achievement celebration popups ARE swipe-dismissable (they still aren't *docked* as bottom-sheets by the sheet CSS — they just slide down with the finger and close). `dismissSheet` fires the overlay's own backdrop-close handler via `ov.click()`, so every swipe-closable overlay MUST carry an `onclick="if(event.target===this) close…()"` backdrop handler — otherwise swipe falls back to a crude `display:none` that skips the close fn's cleanup (draft-restore, promise-resolve, etc.). For this reason `#push-prompt-overlay`, `#library-sortfilter-overlay` (→ `closeLibrarySortFilterModal({restoreDraft:true})`), and `#prior-watches-overlay` (→ `closePriorWatchesModal()` = cancels the save flow) were each given a backdrop handler. **When adding ANY new `.auth-overlay` modal, give it a backdrop-close handler** or its swipe-to-dismiss won't clean up. Drags that start on an `input`/`textarea`/`select`/`contenteditable` are ignored. **Equal-size top-row buttons (mobile):** each page's top control group stretches its buttons to equal width — `.library-controls-row` + `.lists-detail-toprow` (existing), plus `.feed-controls-actions` (Feed's Follows/Filter wrapper) and `.achievement-filter-wrap` (Achievements' Filter/Sort), all via `> button { flex: 1 1 0 }`. **Mobile page-layout overhaul:** a ≤768px block (scoped to `#app-root`) flattens the `.page-title-card` header (transparent, no card chrome, smaller `h1`/subtitle), drops the inline 2rem/3rem `.container` top/bottom padding, and tightens the `mb-6` header gap (via `:has(.page-title-card)`, with a `.lists-header-row` fallback) so Feed / My Movies / Lists / AI Picks / Data Dash / Achievements / Log form stop wasting space and read seamlessly. **AI Picks / Data Dash / Achievements fully HIDE their redundant in-page title+subtitle on mobile** (the header bar already names the page) — `body[data-page="dashboard"|"achievements"] #app-root .page-title-card { display:none }` and `body[data-page="ai_picks"] #app-root .container > .glass-panel:first-child { display:none }` (AI Picks' header is now a `.page-title-card`, but it's also the first `.glass-panel` child of `.ai-container`, so this first-child selector still hides it); **Account** also hides its `.page-title-card`. **All these selectors stay `#app-root`-scoped on purpose:** an unscoped `…[data-page] .container {…}` rule also matches the fixed navbar's `.container.nav-inner` and shoves the header-bar text down (that bug). The header→first-content top gap is unified to **0.6rem** on every page (`#app-root .container { padding-top: 0.6rem }`). Home is intentionally left alone. **Active-filter highlight:** filter/sort buttons across Feed / My Movies / Lists / Achievements get the vibrant **`.filter-active`** class (solid brand fill + a little white **dot** via `::after`, never see-through) when a filter (or sort) is non-default — the Filter button lights up for active filters, the Sort button for non-default sort. **No see-through buttons:** the button classes are opaque — `--btn-outline-bg` is a solid `#202024`, `.btn-glass`/`.choice-btn`/`.nav-auth-btn` use solid fills, and the inline red "delete"/accent button fills were converted from low-alpha rgba to solid colors. **Pull-to-refresh** (`initPullToRefresh`/`pullToRefreshAction`/`pageSupportsPullToRefresh` in `09-home-ui.js`): drag down at the top of a mobile page to reload it — the page content (`#app-root`) **drags down with the finger** (damped) and snaps back, with the `#ptr-indicator` spinner riding along. Opted in for **Feed** (`loadFeedPage`) + **My Movies** (`loadLibraryPage`); extend by editing those two helpers. **Per-page mobile redesigns (one page at a time):** **Feed** hides its redundant in-page title row (`.feed-title-row`), the "Following Activity"/`#feed-meta` heading (`.feed-activity-heading`), and the **Refresh** button (→ pull-to-refresh); its Filter modal hides grey help text (`.feed-filter-help` + `.feed-filter-toggle-sub`). **My Movies** mirrors it: hides the title row (`.library-title-row`), the "Recent Watches"/`#library-meta` heading (`.library-section-heading`), and **Refresh** (`#library-refresh` → pull-to-refresh), and flattens the wrapper panel (`.library-panel`) so the movie cards aren't a container-in-a-container (keeps the slim full-bleed gutter). The old combined **Filters/Sort** button is split into separate **Filters** and **Sort** buttons (`open_filters`/`open_sort` actions) — both open the one `#library-sortfilter-overlay` modal via `openLibrarySortFilterModal(mode)`, which shows only the `[data-sf="sort"]` or `[data-sf="filters"]` section (state still saves together). The view control is **one button** (`#library-view-toggle-btn`, `toggle_view` action) showing the view you'll switch TO (label flips List View ⇄ Grid View via `syncLibraryViewUI`); it shares **one full-width row** with Filters + Sort (`.library-controls-row`, each `flex:1`). **List view** renders compact **Feed-style cards** (`.library-feed-card`, reusing `.feed-card-row/-poster/-main`) and tapping a card opens the same `openLibraryMovieModal` diary popup (Edit/Delete/Recommend live in the modal); **Grid view** stays the poster grid. `libraryViewMode` ('list' default | 'grid') is honored on mobile (no more forced grid). The website-y **`<footer>`** (copyright + social icons) is fully hidden on mobile. **Lists** mirrors the same pattern: hides the title card + the active-list heading/grey subtitle (`.lists-active-heading`) and flattens the data panel (`.lists-panel`); the New List / list-picker top controls stay. **Identical spacing:** Feed / My Movies / Lists share one mobile rule so the header→buttons gap (`.container` `padding-top`) and the buttons→data gap (the items container `margin-top`) are the same value (0.6rem) on all three, with the wrapping panels adding no extra top padding. **Tab-bar badges:** the Feed/Lists unseen-count badges (`nav-badge-feed-t`/`nav-badge-lists-t`, fed by `setNavBadge`'s `-t` twins) are direct children of their `.tabbar-btn` (class `.tabbar-badge`) positioned at the individual button's top-right corner. **Mobile Data Dash overhaul (swipe + bento):** `initDashSwipeNav` (IIFE here) lets you **swipe left/right anywhere on the Data Dash to move between the six sections** (`general → ratings → tiers → favorites → charts → quotes`, no wrap) by calling `setDashboardTab`; mostly-vertical drags fall through to scrolling and swipes that start inside a horizontally-scrollable child (the Activity chart) are ignored. The six **tab pills become a sticky "dots + centered label" position indicator** at the top (styled in a ≤768px block in styles.css): the active section renders as a highlighted pill showing its name while the other five collapse to small dots (`.btn-outline` → 8px dot via `::before` inside a 24px tap target, `.btn-glass` → labelled pill), the whole cluster centered — e.g. `General • • • • •` / `• • Tiers • • •`. `dashCenterActivePill()` (also here) is a harmless no-op now the row no longer scrolls, but is still called by the swipe handler + `setDashboardTab`. Each pane is reflowed into a **compact bento layout** — General order (via `display:contents` on `.dash-gen-top` + `order`): full-width **share wheel** which on mobile is a **two-view card** (`.dash-general-pie-card[data-pie-view]`): a **Chart/List toggle button** (`#dash-pie-view-toggle`, `initDashboardGeneralPieViewToggle` in `06`) swaps between the **wheel** (default `chart` view, ~200px) and the **legend as a clean full-width list** (`list` view) so they never crowd the card at once (desktop still shows both side by side). The MPA/Decade/Genre legend chips are **borderless/transparent** (no boxed container; only the selected `.is-active` chip keeps a highlight box). **Tapping a wheel slice** (`initDashboardGeneralPieTap` in `06`, via `dashAngleFromEvent`/`dashFindPieIndex`) highlights it and reveals that slice's data in the `#dash-pie-segment-detail` chip (label / share% / count); tapping the chip drills into My Movies filtered by the slice (shared `dashNavigateToPieSegment`, also used by the legend-row clicks). Next is the **watch-method KPI** rendered as a **vibrant brand→accent gradient hero banner** (`.dash-watch-method-card`, big % left + counts right via a 2-col grid, with a soft radial glow `::after`) → the three **Most-Watched** tiles (Director / Movie / Actor as identical clean horizontal list rows: a fixed `--mw-poster-size` (72px) poster flush-left + a left-aligned, vertically-centered text column. Each card's text is wrapped in a `.dash-person-text`/`.dash-most-movie-text` div that is `display:contents` on desktop — so the desktop vertical card is untouched — and becomes a single `flex` column on mobile, which is what makes all three rows align identically rather than drifting to different x-positions; replaced the older `grid-template-areas` approach); Ratings shows highest/lowest director side by side. The mobile control row (`.dash-dashboard-row`) drops the glass-panel card chrome + the "Timeframe"/"Counts"/"Rank by" captions; **Timeframe + Counts become connected iOS-style segmented controls** (`.dash-ctl-pills.dash-seg` — a tinted track of equal connected segments with a gradient-filled active segment, the `dash-seg` class added in the `02-router.js` markup), while **Rank by** (7 options, Favorites tab, `#dash-fav-metric-wrap`) — too many for one row — is laid out on mobile as a **balanced equal-width pill grid** (`flex-wrap` + `justify-content:center`, each pill `flex: 0 0 calc(25% - 6px)`): 4 pills on top, 3 centered below, all the same width and all visible at once (no scroll, no dropdown). The active pill gets the brand→accent gradient fill so it reads as one cohesive surface with the Timeframe/Counts segmented controls. The native dropdowns (`.dash-ctl-select`) stay hidden on mobile. The **Ratings-tab** metric toggle (`#dash-ratings-chart-tab-wrap`) uses the General tab's `.dash-general-pie-toggle` and gets **no special mobile sizing**, so its Genre/Decade/MPA pills render byte-for-byte like the General tab's MPA/Decade/Genre pills (title left, content-width pills in one row right); to keep that single row, the title is shortened to a static **"Avg Rating"** (`dashRatingsChartTitleForTab` in `06`) and the grey meta line (`#dash-ratings-genre-meta`) is hidden on mobile. The **Tiers-tab** poster cards show "Title (Year)" on line 1 and "X% Overall" on line 2 where only the **% is tier-colored** (`.dash-tier-score[data-tier]` reusing the `--tier-*-rgb` vars, like `.dash-tier-pill`) and "Overall" stays white; the tier grid is 3-up on mobile. Mostly CSS (panes get a directional slide-in `.dash-pane-in-left/-right`, `@keyframes dashPaneInLeft/Right`). **One content change (desktop too):** the old **Watch Method stacked-bar chart was replaced by a simple KPI** ("X% — Y of Z watched in theaters") — markup in `02-router.js`'s `renderDashboard` (`.dash-watch-kpi`), populated by `dashSetWatchMethodDisplay` in `07-dashboard-charts.js` (now writes `#dash-general-theater-pct`/`-theater-count`/`-watch-total`/`-home-count` instead of the removed stack bar + at-home/in-theater rows). |
| `10-logging-form.js` | The "log a movie" diary form: watch-method toggle, genre chips, validation, `handleFormSubmit`. **Watch Date / Times Watched / Watch Method are NOT on the main form for new entries** — after the review validates, `handleFormSubmit` opens the post-save **Watch Details** modal (`promptWatchDetails`: when / where / "Was this your first time watching this film?"); answering **"No"** (i.e. NOT first time — internally `watched_before=true`) then opens the **Previous Watches** modal (`promptPriorWatches`) whose own count input drives how many date+method rows appear → bulk `Watch Logs`. ("Yes"/first time skips straight to save.) The **Update Ratings** form no longer shows Date Watched / Times Watched / Watch Method at all (they're set once at first log; adding a watch on update still uses the separate update-watch modals). The **"Movie Details" panel is collapsible** (`toggleSubmitDetails`/`initSubmitDetailsCollapse`, `.submit-details-panel`): the header + poster stay visible; the caret shows an **Expand/Collapse** label (`updateSubmitDetailsToggleLabel`); the metadata fields live in `.submit-details-body` that defaults **open on desktop** and **collapsed on mobile** — except it auto-opens (and red-highlights via `.submit-field-missing` + a "Needs info" badge) when any of Year/MPA/Runtime/Series/Director/Genre is missing (`detailsAnyMissing`, computed in `renderSubmit`). The red highlights **clear live** as each field is filled (`refreshMovieDetailMissingHighlights`); ratings are never highlighted at render — only after a failed save via `blockSubmitWithValidationUI` (which also force-opens the panel). The **Detailed Scoring** number boxes **hard-enforce 0–100 in real time** (`enforceRatingScore` reverts an out-of-range keystroke to the last valid value; `syncScoreFromSlider` mirrors the paired range slider) — `inputmode="numeric"` keypad, `data-last-valid` seeds the initial value. |
| `11-achievements.js` | Achievements + tiers: definitions/loading, popups & animations, rating milestones, admin signup toggle, ratings-success modal. **Admin "Show debug logs button" toggle** (`handleAdminHideLogsToggle`/`syncAdminHideLogsToggleUI`/`applyLogFabVisibility`/`isLogFabHidden`, key `ct_hide_log_fab`): hides the bottom-left `#log-fab` debug button per-device; `applyLogFabVisibility` (called by `refreshAuthStateAndUI` in `14-themes.js`) shows it only for `ADMIN_EMAIL` AND when not hidden. **Badge rendering (icon + automatic tier frame):** `renderAchievementIconHtml`/`achievementIconIsSvg`/`achievementIconToSrc` render an achievement's `icon_url` — which may be raw inline `<svg…>` markup (rendered safely as a `data:image/svg+xml` `<img>`, never innerHTML), a data URL, or an http(s) URL. **SVG icons render as a framed medallion** (`.achievement-icon.is-svg` in styles.css) whose ring/sheen is tinted by the tier's `--tier-rgb`, so **one base icon per achievement/family works for every tier — no per-tier art**. Legacy raster (Canva) icons keep their original full-bleed look. Used by the card grid (`renderAccountAchievements`) + the earned popup. **Locked-card progress bars:** every **locked** card with a known threshold rule shows a brand→accent progress bar + an `N / target` label (`.achievement-progress*` in styles.css) so users can track how close they are. Thresholds live in the module-level `ACHIEVEMENT_PROGRESS_RULES` map (extracted from the detail-modal logic; keyed by achievement NAME — covers the standard ratings/rewatch/director/genre/decade/day/week/streak families, so custom/admin-built achievements simply render without a bar). `achievementRuleProgressValue` maps a rule type → the user's current value off the **achievement-evidence** object and `getAchievementProgressInfo(name, evidence)` returns `{value, threshold}`; the evidence is loaded once (and the grid re-rendered) by `ensureAchievementProgressData`, reusing the same `loadAchievementEvidence` cache as the detail modal. The two meta lines now carry distinct classes (`.achievement-desc` / `.achievement-status-meta`) so the mobile "hide description" rule + the locked-card flex `order` keep working with the appended bar. **Timeframe filter (Achievements sub-tab):** a **This Month / All-Time** pill group (`[data-ach-timeframe]`) drives `achievementTimeframe` via `setAchievementTimeframe`; in `month` mode `getFilteredAchievementsList` keeps only badges earned in the current calendar month (`isAchievementEarnedThisMonth`, off the `userAchievementEarnedAt` map that `loadUserAchievements` now builds from `User Achievements.earned_at`), and the count chip shows "N earned this month". Default `all_time`. **Admin Achievement Builder** (`openAchievementBuilder`/`closeAchievementBuilder`/`runAchievementBuilderGenerate`/`selectAchBuilderIcon`/`renderAchBuilderIcons`/`saveAchievementFromBuilder`, gated to `ADMIN_EMAIL` via the Account admin panel's "Open" button): describe an achievement in plain language → the `generate_achievement` edge action (Claude) returns name/description/`rule` JSON/`family`/suggested tier+points + **real icon options pulled from the Iconify library** (the LLM proposes common object keywords → the edge fn searches Iconify and fetches actual professional SVG icons, recolored light to sit inside the tinted medallion — NOT LLM-drawn SVG, which looked crude). For franchise/filmography prompts it resolves a `movie_set` rule via a **source** so the set can auto-update when new films release: `source_kind` `person` (a director/actor → `person_tmdb_id` + `person_role`, resolved from `/person/{id}/movie_credits`), `collection` (a franchise TMDB groups → `collection_tmdb_id`, resolved from `/collection/{id}`), or `manual` (a curated subset → frozen `tmdb_ids` from the LLM's film list). The builder shows a **source-confirmation panel** (`syncAchBuilderSourcePanel`/`resolveAchBuilderSource`, type select + name + **"Find on TMDB"** → the `resolve_movie_source` edge action) so the admin verifies the right person/collection + the resolved film list before saving; in Edit mode that same button **refreshes** a set's films. For the **manual** kind the panel becomes an editable curator: the AI's suggested films seed a removable-chip list, an **"add movie" search** (`searchAchBuilderManual` → the public `search` action) appends films, × removes them (`achBuilderManualFilms` ↔ rule `tmdb_ids` via `syncManualRuleIds`); editing an existing manual set resolves its stored ids to titles via the `tmdb_lookup` edge action (`loadManualFilmsFromIds`). The resolved source ids + `tmdb_ids` are merged into the Rule JSON (the single source of truth on save). Admin reviews/edits the fields, picks an icon ("More icon options" re-fetches fresh icons via `icons_only` reusing the returned `keywords`, no extra LLM call), and Save writes the row via the `save_achievement` edge action (service-role insert, or **update in place by id** so earned records stay valid). **Tier ladder:** for count/threshold prompts (e.g. "Rate 10 movies"), `generate_achievement` also returns a `ladder` of additional higher sibling tiers (Rate 20/50/100…) — the builder shows them as editable, checkbox-selectable rows (`renderAchBuilderLadder`, state `achBuilderLadder`), and Save creates the primary + all checked tiers in one pass (each its own row, sharing the family + chosen icon + base rule with its own threshold; the Save button label shows the count). Ladders only apply in Create mode. **Two modes (`setAchBuilderMode`):** "Create new" (prompt → generate) and **"Edit existing"** (`syncAchBuilderEditOptions`/`loadAchievementIntoBuilder` — pick a current achievement from a dropdown, its fields + current icon load into the form, regenerate just the icon or tweak fields, Save UPDATEs the same row = **replaces, no duplicate**). An **"Apply this icon to every tier in this family"** checkbox sends `apply_icon_to_family` so one save re-skins all sibling tiers (e.g. all "Watch N movies") at once — the fast way to migrate the old Canva icons. **Earned-popup celebration (all types, save-driven):** `handleFormSubmit` (`10-logging-form.js`) snapshots the user's earned set before the save (`captureEarnedAchievementIds`) and calls `popNewlyEarnedAchievements(uid, before)` after — it reloads `User Achievements` (the DB triggers already granted any newly-qualified achievement of ANY type during the insert) and animates ONLY the brand-new ids vs the snapshot. So every achievement type can celebrate (not just rating milestones), and retroactive/admin-added grants never animate. (Replaced the old rating-only `checkAndAwardRatingMilestones`, now unused.) New achievements created in the builder are swept to all already-qualifying users **silently** (the `save_achievement` edge action calls the `award_all_users()` SQL fn on insert) so they're not mis-detected as "newly earned" later. Modal markup = `#ach-builder-overlay` in index.html. The `family` key groups sibling tiers so they share one icon; awarding (incl. retroactive) + tier recalc is done by Postgres triggers (see `achievements_rules_update.sql` / `achievements_overhaul.sql`), not JS. |
| `12-watch-modals.js` | Update/delete watch & rating modals, watch-method & prior-watches prompts, the new-entry **Watch Details** prompt (`promptWatchDetails` + `select/submit/closeWatchDetailsModal`), the count-driven **Previous Watches** modal (`promptPriorWatches`/`renderPriorWatchesRows`/`submitPriorWatchesModal`), loading overlay, **DB helpers** (`insertWatchLog*`, `getDbMovieIdByTmdbId`, `callSwiftApi`, `callColorThemeEdge`, `recomputeMyTasteProfile` — best-effort fire-and-forget rebuild of the caller's taste profile via the `swift-responder` edge fn, called after every rating save in `10-logging-form.js` + before AI Picks, etc.) |
| `13-auth-guest.js` | Guest/demo mode (`enterGuestMode`/`exitGuestMode`), `getActiveUserId`, `guardGuestWrite` |
| `14-themes.js` | Theme system: load/apply themes & colors, background images, theme-creator data layer, help/feature popups, `refreshAuthStateAndUI` |
| `15-auth-account-modals.js` | Auth modal (login/signup/logout/forgot-password), account-section modals (`openAccountSectionModal(kind)` for `profile`/`notifications`/`security`/`feature`), username validation. **Signup no longer collects a Display Name** (set null) and the **"Forgot password?" link is hidden in Sign Up mode** (only shown when logging in). `display_name` is no longer editable anywhere in the UI — existing values are left untouched (the column still exists + drives the nav fallback name). On a successful signup it sets `localStorage.ct_prompt_push_signup='1'` so the app prompts to enable push on the first authenticated boot. **Multi-device:** both `signOut()` calls use **`{ scope: 'local' }`** so logging out (or the invalid-token cleanup) on one device never revokes other devices' sessions. NOTE: if logging in on a new device still kicks out other devices, that's the Supabase **dashboard** setting *Authentication → Sessions → "Single session per user"* (server-side; can't be changed in client code — turn it OFF). |
| `16-ai-picks.js` | AI Picks page (redesigned — single-column **composer** in `renderAiPicks` (`02-router.js`): a prompt card, a **Refine** card holding two matching equal-width pills (`.ai-refine-pill`) — the **Filters** button (`#ai-open-filters-btn` → `openAiFiltersModal`; `syncAiFiltersButton` adds the app-standard `.filter-active` + a count when filters are set, re-synced on init + on filters-modal close) and a **Hide watched** toggle — plus the inline **similar-movie** search (unchanged), then a full-width **Generate Picks** button (the **Debug** toggle next to it is **admin-only** — `initAiPicksPage` shows `#ai-debug-wrap` only when `cachedAuthUser.email === ADMIN_EMAIL`). The filters modal's genre + watch-option chips render in a compact `.ai-chip-grid` (small buttons, even grid) instead of a long wrapping list. The **process/validation is unchanged** — Generate still requires complete filters OR ≥1 similar movie). `initAiPicksPage` binds the delegated listeners (vibe chips, genre/provider toggles, similar search, generate/clear/rerun, plus the result-card actions). **Results** (`showAiResults`, cached in `aiLastResults`): a responsive `.ai-pick-card` grid — poster + rank, title (year), a tier-colored **`.ai-match-badge`** "NN% match" from each pick's `taste_score` (high≥80 / mid≥60 / low), IMDb + MPA chips, the reason, and an action row: **+ Bucket List** (`aiAddToBucket` → reuses `addCardToBucketList` from `21-swipe-deck.js`) + **Details** (`openAiDetailModal`/`closeAiDetailModal` → `#ai-detail-overlay`, fetches via `callSwiftApiGetMovieDetails` and renders by **reusing `renderBackDetailsHtml({_details})`** from `21-swipe-deck.js`; tapping the poster also opens it). The old "How it works"/"Tips" static cards were removed. Also: `loadingImages`, `initAiPicksPage`. The `ai_picks` edge action is unchanged. |
| `17-theme-creator.js` | Theme Creator UI (backdrop/AI search, selection, save/delete), `initThemeCreatorPage` (gated to `THEME_CREATOR_OWNER_EMAIL`) |
| `18-account-page.js` | **Settings page** (route `settings`, reached via the ⚙ on the fun Account page): load/save profile, change password, feature requests, and the **Achievements** loader (`loadAchievementsPage`) — Achievements is now the second sub-tab of the **Leaderboard** route (`open_achievements` navigates to `leaderboard`); the Account "Leaderboard" card links there. **Profile photo** (`handleAccountIconPick`/`handleAccountIconRemove`/`processAccountIconFile`/`setAccountIconPreview`): the Profile modal shows **one large centered avatar** (`.account-icon-wrap` → `#account-icon-preview`) with a small **camera "upload" button** at its corner (`.account-icon-upload`, `data-account-action="pick_icon"`) + hidden `<input type=file accept=image/*>` (camera roll on mobile) — mirrors the Edit-list cover control; the old "Change photo"/"Remove" text buttons were removed (`handleAccountIconRemove` / `remove_icon` still exist but are no longer wired to any button). The chosen image is client-side center-cropped + downscaled to a ~256px square JPEG **data URL** stored directly in `Users.icon` (no Storage bucket — `renderUserIconHtml`/`isUserIconUrl` in `15-auth-account-modals.js` already render `data:`/`http` icons); on change it updates `Users.icon`, the cached nav avatar (`cachedUserIcon`), and calls `refreshAuthStateAndUI`. **Web Push** opt-in (`savePushSetting`/`enablePushOnThisDevice`/`disablePushOnThisDevice`/`refreshPushToggleState`): lives in its **own "Notifications" account-section modal** (`open_notifications`, separate from Profile) — a "Push notifications" toggle → on Save it requests OS permission, subscribes via `PushManager` (`VAPID_PUBLIC_KEY` from `01-config.js`), and upserts the subscription to `push_subscriptions`. `enableNotificationsTest` (the "Send test" button) calls the `test_push` edge action for a **real end-to-end** push to the caller's own devices, falling back to a LOCAL `showNotification` (and surfacing the server's reason) only when no real push was delivered. **New-user prompt:** `maybePromptPushAfterSignup` (called on boot) shows the `#push-prompt-overlay` welcome modal (`openPushPromptModal`/`closePushPromptModal`/`confirmPushPromptEnable`) when `ct_prompt_push_signup` is set + the user is authed + push isn't already on — "Enable Notifications" triggers the same OS-permission/subscribe flow. The Account page is organized as option cards: **Profile** (username + photo only — display name removed), **Notifications**, **Achievements**, **Feature Requests**, **Security**, Theme, and a red **Log out** card last (`.account-logout-card`). (Email-to-SMS / phone + carrier were fully removed — push only.) |
| `19-logging-boot.js` | Message log + toast (`showToast`, `emitLog`), global error handlers, the **service-worker registration + "New version available" update prompt** (`showUpdatePrompt`, fired by the SW's `SW_ACTIVATED` message), **push-notification deep-link routing** (`handleNotificationRoute`: sends the app to Feed on `#feed` / the Recs list on `#recs`, opens a **user's profile** on `#user=<userId>` (new-follower push → `openUserProfile`), opens **a specific user's review** on `#review=<userId>:<movieId>` (the "your rec got watched" push → `openProfileMovieReview`), or opens **a specific list** on `#list=<listId>` (the shared-list "movie added" push → sets `listsPendingSelectId`) — used both on cold start from the URL hash and while already running via the SW's `NOTIFICATION_NAV` postMessage; this is what makes the badge clear on tap), the **new-user push prompt** trigger (`maybePromptPushAfterSignup`, run once auth is resolved on boot), and the **boot sequence** (`DOMContentLoaded`, auth-state listener). Must load last. |
| `20-leaderboard.js` | **Leaderboard** sub-tab of the Leaderboard route (`#lb-panel-leaderboard`). A **friends leaderboard** — ranks YOU + the people you FOLLOW via the `get_follow_leaderboard()` SECURITY DEFINER RPC (`leaderboard.sql`), so no global stats are exposed and `privacy_level` never has to be consulted. Controls: **metric** (`leaderboardMetric` — Movies Rated / Achievement Points, `[data-lb-metric]`) × **timeframe** (`leaderboardTimeframe` — This Month / All-Time, `[data-lb-timeframe]`, hidden for the all-time-only Points metric). `initLeaderboardPage` binds one delegated click listener (sub-tab switch, metric/timeframe pills, the Achievements sub-tab's `[data-ach-timeframe]` This Month/All-Time pills → `setAchievementTimeframe`, row→`openUserProfile`); `setLeaderboardSubtab`/`setLeaderboardMetric`/`setLeaderboardTimeframe` drive state; `loadLeaderboard` calls the RPC (with an out-of-order-response token guard) and `renderLeaderboardContent` paints a **top-3 podium** + ranked rows (`.lb-podium`/`.lb-row`, reusing `renderUserIconHtml` + `escapeHtml`, self-row highlighted). Loaded **before** `19-logging-boot.js` despite the higher number (19 must stay last). Styles in the "Leaderboard page" block at the end of `styles.css`. |
| `21-swipe-deck.js` | **Discover swipe deck** (route `discover`, reached from the mobile "More" sheet + the desktop nav link; view template `renderDiscover` in `02-router.js`, init `initDiscoverPage`). A lean-back "what to watch" surface: one movie **card** at a time, **swipe RIGHT** = add to Bucket List + record a `right` swipe; **swipe LEFT** = record a `left` swipe (soft-skip, resurfaces after 30 days). Cards + ranking come from the `swift-api` **`swipe_deck`** action (pool = your top-affinity genres + trending + all-time popular, scored by `predictTasteScore`, excludes watched / right-swiped-ever / left-swiped-<30d / already on the user's **Bucket List OR Recs** list). `loadDiscoverDeck` fetches batches of 25 (prefetches more when ≤5 remain), `renderDiscoverStack` paints up to 3 stacked cards (top is interactive) but **gates the top card on its full details being loaded** (`discoverDetailsReady` — it shows the loading spinner until the card's `_details` incl. the IMDb rating are in, so the poster never appears before the rating; `discoverEnsureDetails` re-calls `renderDiscoverStack` once the top card is ready). `buildDiscoverCardHtml` builds a flip card (front: poster + `taste_score` badge + title/genres + the **IMDb** rating from `_details.imdb_rating_pct` (NOT TMDb) + one taste reason; back via `discoverFlipTop`: overview). To kill swipe lag, `discoverPreloadAhead` prefetches full details for up to `DISCOVER_DETAILS_AHEAD` (10) cards BEHIND the current one (not just top+next), so fast swiping always lands on a ready card. Gestures: module-scoped drag state + window mouse listeners bound ONCE (touch listeners die with the element), threshold 100px → `discoverSwipe(dir)`; on-screen Skip/Details/Like buttons call the same (the Skip/Like buttons each sit in a `.discover-action-col` whose tiny artistic gesture hint `.discover-hint` — red "not for me" / green "into the bucket" — is **absolutely positioned UNDER the button** so it never shifts the centered button row; the right-swipe "Added to Bucket List" toast is intentionally brief, `durationMs: 900`). While dragging, a full-card **colour wash** fades in proportional to pull — green **"Add to Bucket List"** right, red **"Dismiss"** left (`.discover-swipe-overlay`, opacity set in `discoverDragMove`). `recordSwipe` upserts to the **`swipes`** table directly (RLS = own rows); `addCardToBucketList` reuses `ensureBucketListForUser`+`ensureMovieFullySyncedForLists`+`addMovieToList` (so right-swiped movies enter the catalog WITH keywords). **Phase 2 (done):** the `swipe_deck` pool also includes a **friends source** (movies people you follow rated ≥85) and each card carries **social proof** — `card.friends` `[{name,icon,overall}]` for follows who rated it 85+, rendered as an overlapping-avatar chip (`discoverSocialHtml`, reuses `renderUserIconHtml`). The **back of the card shows full details** (Director / Cast / Runtime / Rated / Genre / IMDb + overview) via `discoverEnsureDetails` → the `details` edge action → `renderBackDetailsHtml`, **preloaded up to 10 cards ahead via `discoverPreloadAhead`** so the back is ready BEFORE the flip (no spinner); it patches that card's `.discover-back-details` + the front `.discover-front-imdb` badge in place (no full re-render). **Tapping the card** (a tap, not a drag — detected in `discoverDragEnd`) flips it: front → details, and tapping the details side flips back (same as the ⓘ button → `discoverFlipTop`). There's no "Back" button on the details side (removed — tap to flip back). The back face is **opaque** (`#16161a`, NOT `--glass-bg`); only the **overview** text scrolls when long (the details grid stays put). **Phase 3 (done):** swipes feed a **weak genre "appeal" signal** into taste — `recordSwipe` stores the card's `genres` on the swipe row (so left-swiped movies, which never enter the catalog, still contribute), `taste_profile_edge.js` computes a per-genre right-swipe-rate **lift** into `swipe_genre_affinity_json`, and `predictTasteScore` blends it at a low weight (`TASTE_SWIPE_GENRE_WEIGHT`). `discoverFlushAppeal` recomputes the profile once per deck session (on empty + on leaving Discover via `router.navigate`), not per swipe. Styles in the "Discover swipe deck" block at the end of `styles.css`. |
| `22-account-fun.js` | **Fun Account page** (route `account`; shell `renderAccount` in `02-router.js`, init `initAccountHome`/`loadAccountHome`). A personal "about you" page (not settings): a **big tappable avatar** with a camera button (`data-account-home-action="pick_icon"` → shared `#account-home-icon-file` → `handleAccountHomeIconPick` reuses `processAccountIconFile` from `18`, saves to `Users.icon`), username + **tier badge + achievement points** (from `Users.tier_id`→`User Tiers` + `achievement_points`), an **editable Bio** shown right under the username **inside the hero** (`Users.bio` — the bio line itself is the tap target, `data-account-home-action="edit_bio"` → the `#account-bio-overlay` small modal: `openBioModal`/`closeBioModal`/`saveBio`/`updateBioCounter`, 280-char limit), **Following / Followers counts** (two `count` queries on `Follows`; the stat buttons open the **`#account-follows-overlay` list modal** — `openFollowsModal(kind)`/`closeFollowsModal`/`loadFollowsList(kind)` query `Follows` for the relevant ids + `Users` for name/icon, render tappable rows that `openUserProfile(userId)` (action `open_user`); rows in the **Following** list also carry an **Unfollow** button (`handleUnfollowFromModal` deletes the `Follows` row, drops the row + decrements the page's Following count)), an **AI taste blurb** in its OWN card below the hero (`#account-home-blurb-card`, hidden when empty; text from `Taste Profiles.taste_blurb`, generated + cached by `taste_profile_edge.js`, with `deriveTasteBlurb` as the client-side fallback until the profile is recomputed), and a **compact Taste card** (`renderAccountHomeTaste` reads the `Taste Profiles` row: rater style from `mean_overall`, top genres by `aff`, favorite decade, top director/actor, vs-IMDb-crowd; "Data Dash →" link). A ⚙ **Settings** button (top-right) navigates to the `settings` route. All writes go through `guardGuestWrite`. Loaded **before** `19-logging-boot.js` despite the higher number (19 must stay last). Styles in the "Fun Account page" block at the end of `styles.css`. |

To regenerate this index after edits:
`grep -rnE "^        (async function|function|class) " assets/js/`

## Running & testing locally

- Serve from the project root (paths are relative):
  `python3 -m http.server 8080` then open `http://localhost:8080`.
  (VSCode `launch.json` is also set to `http://localhost:8080`.)
- **Primary debug signal:** the browser DevTools **Console**. A broken script
  load shows as a 404/net error; a missing global shows as
  `ReferenceError: X is not defined` when the relevant UI is used.
- There is no automated test suite and no JS runtime installed locally (no
  node/deno) — validation is manual in the browser.

## Back end (reference, in repo root)

These are deployed to Supabase, not loaded by the front end, but document the API
the front end calls:
- Edge Functions: `EdgeFunc` (deployed as `swift-api`), `edgefunccopy`,
  `color_theme_edge.js`, `taste_profile_edge.js` (deployed as **`swift-responder`**;
  the front end calls these via
  `callSwiftApi*` / `callColorThemeEdge` in `12-watch-modals.js` and
  `08-search-trending.js`). **`taste_profile_edge.js` computes & upserts the
  `Taste Profiles` row** (core compute is the reusable `computeAndStoreTasteProfile(supabaseAdmin, userId)`): mean/median/std overall, like_threshold (75th pct),
  imdb_delta, subrating correlation weights, runtime/decade bins, **per-person and
  per-genre affinity** — every group average is **Bayesian-shrunk toward the user's
  mean** (`SHRINK_K=5`) so sparse groups don't read as strong signal; people require
  ≥2 rated movies. Also computes **swipe-appeal** (`swipe_genre_affinity_json`): a
  WEAK per-genre right-swipe-rate **lift** from the Discover deck's `swipes`, kept
  separate from the ratings-based affinity. Also generates the cached **AI taste
  blurb** (`Taste Profiles.taste_blurb`) — a Claude call (`ANTHROPIC_API_KEY`,
  model `ANTHROPIC_MODEL_BLURB`, default `claude-haiku-4-5-20251001`) that turns the
  computed signals into a one-line taste snippet for the fun Account page; it's
  best-effort (omitted from the upsert on failure so the prior value persists, and
  skipped under ~3 ratings). **Two ways it stays fresh, no longer tied to opening AI Picks:**
  (1) **event-driven** — `recomputeMyTasteProfile()` (in `12-watch-modals.js`) is fired
  fire-and-forget after every rating save/edit in `handleFormSubmit` (`10-logging-form.js`)
  and still right before an AI Picks run (`16-ai-picks.js`); it POSTs `{user_id}` with the
  caller's bearer so the function rebuilds just that user (auth check: a caller can only
  recompute themselves). (2) **cron/batch** — a `CRON_SECRET`-gated mode (send the
  `x-cron-secret` header, no user session): `{limit, concurrency}` recomputes the
  **stalest-profile-first** batch of all raters (`fetchTasteRecomputeTargets`; `limit:0`
  = everyone, which is also the one-time backfill), driven by
  `.github/workflows/refresh-taste-profiles.yml` (daily 08:30 UTC; repo secrets
  `SUPABASE_TASTE_FUNCTION_URL` = the `…/functions/v1/swift-responder` URL +
  `SUPABASE_PUBLISHABLE_KEY` + `CRON_SECRET`, and the edge secret `CRON_SECRET`).
  `EdgeFunc` dispatches by `body.action`; notable actions
  include `search`, AI picks (the `ai_picks` action **reads that Taste Profile and
  scores every candidate with `predictTasteScore(model, features)`** — a reusable
  0-100 prediction from genre+decade affinity (+ keyword/theme affinity when a movie's
  keywords are known — inert for AI Picks discover candidates, ready for the per-movie/swipe
  predictor) + a crowd/imdb_delta anchor + a WEAK swipe-appeal genre nudge
  (`swipe_genre_affinity_json`, Phase 3), with short
  `taste_reasons`; candidates are sorted by it, the rerank LLM is told to treat it as
  a primary ranking factor (not a tie-breaker) and weave the reasons into its
  explanations, **but the PROMPT outranks taste** (mood-dependent — an explicit
  against-taste request follows the prompt), and `taste_score` is returned on each pick. No-genre "surprise me"
  prompts also lean toward the user's top-affinity genres), `swipe_deck` (powers the
  Discover swipe deck in `21-swipe-deck.js` — returns a ranked batch of unseen movie
  cards from TMDB: pool = the caller's top-affinity genres + trending-this-week +
  all-time highly-voted classics, scored by `predictTasteScore` with a little random
  jitter for variety, excluding movies already rated/logged, right-swiped ever,
  left-swiped in the last 30 days via the `swipes` table, or already on the caller's
  **Bucket List or Recs** list (resolved `Movie Lists`→`Movies.tmdb_id`); no LLM. **Phase 2:** also pulls
  a **friends source** (+ per-card social proof = follows who rated it 90+) via the
  `get_follow_rated_movies` SECURITY DEFINER RPC (`follow_rated_movies.sql`, run as the
  caller through `supabaseUserClient`), `send_recommendation` (adds a movie to each
  recipient's "Recs" list, then sends a **Web Push** via `sendPushToUser` if the
  recipient has `push_subscriptions` rows), `notify_new_review` (web-push every
  FOLLOWER when the caller posts a **brand-new** review — fired fire-and-forget by
  `handleFormSubmit` in `10-logging-form.js` only on the new-entry insert path,
  never on updates; body = `Check out @user's new review of "Title"`,
  `url:"/#review=<authorId>:<movieId>"` → deep-links to the author's diary entry for
  that movie via `openProfileMovieReview`, not just the feed), `notify_new_follower` (web-push the person you just followed —
  fired fire-and-forget from the Feed follow button in `05-feed-library.js`; body =
  `@you started following you`, `url:"/#user=<followerId>"` → opens their profile),
  `notify_rec_reviewed` (web-push everyone who **recommended this movie to the
  caller** when the caller rates it — fired fire-and-forget by `handleFormSubmit`
  on the new-entry path alongside `notify_new_review`; reads `Recommendations`
  where `to_user_id=caller, movie_id=movie`; body = `@caller reviewed "Title" you
  recommended — check it out`, `url:"/#review=<raterId>:<movieId>"` → deep-links to
  the rater's review of that movie), `notify_list_add` (web-push the **other members
  of a shared list** when a member adds a movie — fired fire-and-forget from
  `addMovieToList` in `04-lists.js` whenever the active list is shared; verifies the
  caller is a member, recipients = owner + `List Members` minus the adder; body =
  `@adder added "Title" to <List>`, `url:"/#list=<listId>"` → opens that list),
  `test_push` (sends a real Web Push to the caller's own
  subscribed devices), `generate_achievement` + `save_achievement` (**admin-only**,
  gated to edge env `ADMIN_EMAIL` (default `landon.talus@gmail.com`) — power the
  Achievement Builder: `generate_achievement` calls Claude (`ANTHROPIC_MODEL_BUILDER`,
  default `claude-sonnet-4-6`) to return an achievement draft (name/description/`rule`/
  `family`/tier/points) + a list of icon-search **keywords**, then pulls real SVG icons
  from the **Iconify** library (`api.iconify.design` search + `.svg?color=` fetch, no key —
  helpers `iconifySearch`/`iconifyFetchSvg`/`iconsFromKeywords`; prefers filled/badge-style
  sets, recolors them light). **Iconify search is literal** ("lightsaber"→0 but "light-saber"/
  "saber"→hits), so `expandKeywordVariants` expands each keyword into space/hyphen/concat/
  head-noun/plural forms before searching (plus a hyphen-position probe fallback that splits a
  GLUED compound the LLM didn't separate — "lightsaber"→"light-saber"→`game-icons:light-sabers`),
  and the LLM is told to use common object nouns (written as separate words) +
  generic fallbacks (proper nouns never become icons — "all Nolan films"→film/reel/clapperboard/
  director-chair). Resolves any franchise/filmography film list to real TMDB ids for a
  `movie_set` rule. (`icons_only:true` + `keywords` re-fetches icons with no LLM call —
  the builder's **editable "Icon search terms" field** lets the admin override what's searched; a manual
  search **replaces** the icon grid (not appends) and runs in **strict** mode — only exact format
  variants of the typed phrase, no head-noun broadening — so e.g. "light-saber" returns light-sabers,
  not swords/saber-tooth. Auto AI keywords still search in broad mode.)
  `resolve_movie_source` (re-resolves a `movie_set` source — a TMDB person/collection — to its
  current film list so the builder can confirm/refresh before saving); `tmdb_lookup` (admin-only —
  resolves a list of TMDB movie ids → title/year/poster so a stored manual set shows as editable chips);
  `save_achievement` upserts the `Achievements` row with the service role —
  **update by id keeps earned records valid**, and `apply_icon_to_family:true`
  pushes one new icon to every sibling tier), and `refresh_imdb_ratings` (**cron**, gated by an `x-cron-secret`
  header == `CRON_SECRET` env, no user session — re-pulls OMDb IMDb ratings for the
  ~300 STALEST movies by `Movie External Ratings.fetched_at` and overwrites them in
  place, so ratings drift over time; daily runs rotate through the whole catalog).
  `refresh_imdb_ratings` is triggered by **`.github/workflows/refresh-imdb.yml`** — a
  GitHub Actions daily cron that just `curl`s the Edge Function (the function does the
  DB writes). Needs GitHub repo secrets `SUPABASE_FUNCTION_URL` (the `…/functions/v1/swift-api`
  URL), `SUPABASE_PUBLISHABLE_KEY` (the `sb_publishable_…` key, same one in `01-config.js`;
  swift-api has Verify-JWT OFF so only the `apikey` header is needed — no Authorization
  bearer / deprecated anon key), `CRON_SECRET`; and edge secrets `CRON_SECRET` (same value)
  + `OMDB_API_KEY`. **Notifications are push-only** — the old email-to-SMS path
  (Gmail SMTP / `denomailer` / carrier gateways / `test_sms` / `Users.phone`+`carrier`)
  was removed. All push payloads carry a numeric `badge` from `computeUnseenBadge()`
  (= **newly-posted** feed reviews — `Movie Ratings.created_at` since
  `Users.feed_seen_at`, so edits of old reviews don't count — + unseen recs since
  `Users.recs_seen_at`), which the service worker mirrors onto the iOS home-screen
  app-icon badge. The diary save also sends an `mpa` override so user-entered MPA
  persists when TMDb lacks it.
  - **Web Push** (`npm:web-push@3.6.7`): edge secrets `VAPID_PUBLIC`,
    `VAPID_PRIVATE`, optional `VAPID_SUBJECT` (mailto). PUBLIC key also lives in
    `01-config.js` as `VAPID_PUBLIC_KEY`. Subscriptions in `push_subscriptions`
    (RLS, see SQL). The service worker's `push` handler shows the notification.
- SQL: `dashboard_rpc.sql`, `achievements_*.sql` (the data-driven achievement
  engine: `achievements_rules_update.sql` adds the `Achievements.rule` jsonb column,
  populates rules for the standard achievements, and installs `award_achievements_for_user()`
  + triggers on `Movie Ratings`/`Watch Logs` that award achievements **automatically and
  retroactively** and recalc tiers — awarding is server-side, NOT JS;
  `achievements_overhaul.sql` (**run once**, idempotent) adds the `Achievements.family`
  text column (groups sibling tiers so they share one icon) and a new **`movie_set`**
  rule type — `{"type":"movie_set","tmdb_ids":[…],"require_count":N}` (plus optional source
  metadata for auto-refresh: `source_kind` person/collection/manual, `person_tmdb_id`+`person_role`
  or `collection_tmdb_id`, `source_name` — only `tmdb_ids`/`require_count` are used for awarding) —
  earned when the user has rated a specific SET of films (powers franchise/filmography achievements like
  "watch all 9 Star Wars movies"; the admin builder fills the tmdb_ids), plus an
  `award_all_users()` SECURITY DEFINER fn that silently sweeps grants to every qualifying user
  (called by `save_achievement` on new-achievement insert so retroactive earns don't later animate));
  `achievements_social_rules.sql` (**run once**, after the overhaul — extends `award_achievements_for_user`
  with more rule types: `recommend_count` (Recommendations sent), `follow_count`/`follower_count` (Follows),
  `list_count` (custom Lists, excl. Bucket List/Recs), `theater_count` (Watch Logs watch_method),
  `actor_count` (Movie Cast, like director_count), `quote_count` (fav_quote), `series_count` (Movies.is_series),
  `runtime_hours` (sum runtime/60, threshold in hours), `high_rating_count` (Movie Ratings overall ≥ `min_overall`,
  default 90) — and adds triggers on Recommendations/Follows/Lists so these award promptly, not just on
  rating/watch changes));
  `achievements_cascade_delete.sql` (**run once** — adds an `ON DELETE CASCADE` FK so deleting an
  `Achievements` row auto-removes its `User Achievements` rows and recalcs tiers, touching nothing else)),
  `lists_schema.sql`,
  `library_views.sql` (`user_library_items_v2` — flattened My Movies rows),
  `lists_views.sql` (`user_list_items_v1` — same idea keyed on `Movie Lists`, so
  the Lists page reads a whole list in one query; mirrors the library view's
  owner-rights security model — the app always filters by `user_id`),
  `lists_cover_column.sql` (adds the optional `Lists.cover` text column — a
  per-list square JPEG data URL for the Lists overview cover grid; idempotent,
  reuses the existing `lists_update_own` RLS policy),
  `lists_branded_covers.sql` (**run once** — stores the branded **Recs** /
  **Bucket List** cover art as 320px-square JPEG data URLs in the DB instead of
  forcing large local PNGs from the front end: creates a `list_cover_defaults`
  lookup table (one row per branded list name), backfills `Lists.cover` for every
  existing auto-managed list, and is the source `signup_system.sql` reads to set
  the cover on new users' default lists; idempotent — re-run to re-brand),
  `shared_lists.sql` (**run once**, idempotent — **shared/group lists**: adds
  `Lists.is_shared` + `Movie Lists.added_by` columns and a `List Members` table
  `(list_id, user_id, added_by, role)`; a `public.is_list_member(uuid)`
  SECURITY DEFINER helper (`auth.uid()` is the owner OR a member — definer so it
  can't RLS-recurse); and permissive `*_member` RLS policies on `Lists` /
  `Movie Lists` / `List Members` that let **any member** read/insert/delete a
  shared list's rows. Rows for a shared list keep the OWNER's `user_id`, so the
  owner-keyed `user_list_items_v1` view + existing reads work unchanged),
  `user_tiers.sql`, `cascades.sql`, `recs_and_profile.sql`
  (auto "Recs" list per user + `Users.phone`/`Users.carrier` — the phone/carrier
  columns are now **unused/deprecated** since email-to-SMS was removed; left in
  place, harmless),
  `recommendations_tracking.sql` (`Recommendations` table + unique movie-per-list;
  RLS: read recs you sent/received + **delete recs you received** so removing a
  movie from your Recs list lets it be recommended again + **read the recommender's
  `Movie Ratings` row for the recommended movie** `movie_ratings_select_recommenders`
  so the Recs poster viewer can show their review under strict RLS),
  `search_users.sql` (`search_users(p_query)` SECURITY DEFINER RPC — Feed people-search
  matches **username OR email** by joining `auth.users`; returns only safe columns,
  never the email),
  `leaderboard.sql` (`get_follow_leaderboard(p_metric, p_timeframe)` SECURITY DEFINER
  RPC — powers the **friends Leaderboard** (`20-leaderboard.js`): ranks `auth.uid()` +
  the people they follow by `movies_rated` / `achievement_points` / `watches`, scoped to
  `all_time` or the current `month`. Counts other users' `Movie Ratings`/`Watch Logs`
  (which RLS would hide) but returns ONLY aggregate scores + safe public profile columns,
  and only for people you already follow — so no global stats leak and `privacy_level`
  isn't consulted),
  `push_subscriptions.sql` (Web Push subscription rows + RLS; one per opted-in
  device, read by the edge fn with the service role to send pushes),
  `signup_system.sql` (**authoritative, idempotent signup provisioning — run this
  for new-user setup**: a `SECURITY DEFINER` trigger `handle_new_auth_user()` on
  `auth.users` fully provisions each new account server-side — creates the
  `public."Users"` row (username/display_name from signup metadata, `privacy_level`,
  starting tier = lowest `points_needed`, `achievement_points`=0) plus the
  **"Bucket List"** and **"Recs"** lists — each created with its branded `cover`
  read from `list_cover_defaults` (see `lists_branded_covers.sql`; NULL-safe if
  that table is empty). Works regardless of RLS / client auth
  timing / email-confirmation. Also sets `Users` RLS (authenticated read +
  self-insert/update) and backfills missing rows/lists. **Admin gate:** self-service
  signups (client sends `self_signup='true'` in `signUp({ options:{ data } })`) are
  blocked when `Settings.allow_signups` is false; admin/dashboard-created users
  bypass the gate. Supersedes the list triggers in `bucket_list_auto.sql` /
  `recs_and_profile.sql`, which remain harmless),
  `notification_seen_columns.sql` (`Users.feed_seen_at` / `Users.recs_seen_at` —
  the "last viewed Feed/Recs" timestamps that drive every unseen-count badge),
  `movie_ratings_created_at.sql` (**run once**, idempotent — adds
  `Movie Ratings.created_at` (default `now()`, NOT NULL) = the review's **original
  post time**, frozen on edit (`updated_at` still records the last edit). The Feed
  (`loadFeedItems`) + the unseen-feed badges (`refreshNavBadges` in
  `05-feed-library.js` AND `computeUnseenBadge` in the `swift-api` Edge Function —
  **redeploy the function** after running) order/filter by `created_at` so editing
  an old rating updates it in place WITHOUT resurfacing it / re-pinging the badge.
  Backfills existing rows to `coalesce(updated_at, watch_date, now())` — exact for
  never-edited rows; `id` is a random UUIDv4 so it carried no usable ordering),
  `swipes.sql` (**run once**, idempotent — the `swipes` table `(id, created_at,
  user_id, tmdb_id, direction 'left'|'right')`, unique `(user_id, tmdb_id)`, RLS =
  own rows only; backs the Discover swipe deck. Keyed by `tmdb_id` since most deck
  candidates aren't in `Movies` yet — only right-swiped ones get added. The client
  inserts directly; the `swift-api` **`swipe_deck`** action reads it to exclude
  already-swiped movies),
  `swipe_appeal.sql` (**run once**, idempotent — Phase 3 of the swipe deck: adds
  `swipes.genres text[]` (the card's genres stored at swipe time, so left-swiped
  movies still contribute genres) + `Taste Profiles.swipe_genre_affinity_json`
  (per-genre right-swipe-rate **lift** vs the user's overall rate, computed by
  `taste_profile_edge.js`, blended into `predictTasteScore` at a low weight — a WEAK,
  separate-from-ratings appeal signal; right-swipes are the signal, left only the
  denominator),
  `follow_rated_movies.sql` (**run once**, idempotent — `get_follow_rated_movies(p_min_overall)`
  SECURITY DEFINER RPC powering the swipe deck's **Phase 2**: returns movies the caller's
  **follows** rated ≥ the threshold (tmdb_id/title/year/poster/genres + rater username/icon/overall),
  used by `swipe_deck` for the friends candidate source + the 90+ social-proof chip. Like
  `get_follow_leaderboard` it only exposes people you follow + safe columns, so RLS isn't bypassed
  for anyone else),
  `taste_genre_affinity.sql` (**run once**, idempotent — adds
  `Taste Profiles.genre_affinity_json`; the rest of the genre-affinity / shrinkage
  enrichment lives in `taste_profile_edge.js` and the predictor in `EdgeFunc`'s
  `ai_picks`, so just re-deploy those two edge functions after running),
  `user_bio_column.sql` (**run once**, idempotent — adds `Users.bio`, the free-text
  profile bio edited on the fun Account page; existing self-update RLS covers it),
  `taste_blurb_column.sql` (**run once**, idempotent — adds `Taste Profiles.taste_blurb`,
  the cached AI taste snippet; **re-deploy `swift-responder` after running** so the
  blurb starts getting written on the next recompute),
  `movie_keywords.sql` (**run once**, idempotent — adds the `Keywords` +
  `Movie Keywords` join tables (TMDB theme tags) and `Taste Profiles.keyword_affinity_json`.
  Ingestion is in `EdgeFunc`: every movie save now fetches keywords
  (`append_to_response=keywords`) via `syncMovieKeywords`, and the cron-gated
  `backfill_movie_keywords` action (`x-cron-secret`==`CRON_SECRET`, body `{limit}`,
  returns `remaining` — re-run until 0) fills the existing catalog, runnable one-click via
  `.github/workflows/backfill-movie-keywords.yml` (loops until done). `taste_profile_edge.js`
  then learns per-keyword affinity and the `ai_picks` rerank matches it against
  candidate overviews — re-deploy both edge functions + run the keyword backfill, then the
  taste backfill, after running the SQL),
  etc.; more in `Supabase Setup/`.
- Gating constants in JS: `ADMIN_EMAIL` (admin panel), `THEME_CREATOR_OWNER_EMAIL`
  (theme creator), `DEMO_USER_ID` (guest mode).

## ⚠️ Database schema — where every field actually lives

**Source of truth: the CSV exports in `Supabase Setup/` (one file per table; the
header row = that table's columns).** Read the relevant CSV header before writing
any query. Do NOT guess column locations.

**The #1 trap: movie metadata is split across tables. `Movies` holds only core
fields. Genre, director/cast, keywords, platforms, and external (IMDb) ratings each
live in their own join tables — they are NOT columns on `Movies`.** Selecting a
non-existent column makes the whole PostgREST query fail and return nothing.

### Tables and columns (exact)

| Table | Columns |
|-------|---------|
| `Movies` | `id, created_at, tmdb_id, imdb_id, title, release_year, runtime_minutes, mpa_rating, is_series, poster_path` — **no genre, no director, no cast, no platform, no imdb rating** |
| `Movie Genres` | `movie_id, genre_id, created_at` → join to `Genres` |
| `Genres` | `id, name` |
| `Movie Keywords` | `movie_id, keyword_id, created_at` → join to `Keywords` (`movie_keywords.sql`; TMDB theme tags) |
| `Keywords` | `id, created_at, tmdb_keyword_id, name` (TMDB keyword/theme, e.g. "heist"; `movie_keywords.sql`) |
| `Movie Cast` | `movie_id, person_id, character, created_at` → join to `People` |
| `Movie Crew` | `movie_id, person_id, job, created_at` → join to `People` (director = `job` = 'Director') |
| `People` | `id, created_at, tmdb_person_id, name` |
| `Movie Platforms` | `movie_id, created_at, platform_id` → join to `Platforms` |
| `Platforms` | `id, name` |
| `Movie External Ratings` | `movie_id, source, rating, fetched_at` (IMDb etc.; no user_id) |
| `Movie Ratings` | `id, watch_date, updated_at, created_at, user_id, movie_id, tier, overall_rating, acting_rating, pacing_rating, sound_rating, imagery_rating, plot_rating, dialogue_rating, notes, fav_quote` (one per user+movie; **no movie metadata here**. `created_at` (added by `movie_ratings_created_at.sql`, NOT in the CSV export) = original post time, frozen on edit; `updated_at` = last-edit time. The Feed + unseen badges order/filter by `created_at` so edits don't resurface; backfilled to `updated_at`) |
| `Watch Logs` | `id, watch_date, user_id, movie_id, watch_method` |
| `Users` | `id, created_at, display_name, privacy_level, username, icon, theme_id, tier_id, achievement_points, phone, carrier, feed_seen_at, recs_seen_at, bio` (`phone`/`carrier` are **deprecated/unused** — email-to-SMS removed; `feed_seen_at`/`recs_seen_at` added by `notification_seen_columns.sql`; `bio` (free-text profile bio shown/edited on the fun Account page) added by `user_bio_column.sql` — none of these three in the CSV export) |
| `Follows` | `follower_id, followed_id, created_at` (follower → followed) |
| `Lists` | `id, created_at, list_name, user_id, cover, is_shared` (unique `(user_id, list_name)`; `user_id` = **owner**; auto special lists: **"Bucket List"**, **"Recs"**; `cover` = optional per-list square JPEG **data URL** (`lists_cover_column.sql`); `is_shared` boolean (`shared_lists.sql`) marks a **group list** co-editable by its `List Members`) |
| `Movie Lists` | `list_id, movie_id, created_at, user_id, added_by` (unique `(list_id, movie_id)` — no duplicate movie per list; `user_id` = the list **owner** even on shared lists; `added_by` (`shared_lists.sql`) = who actually added the movie) |
| `List Members` | `id, created_at, list_id, user_id, added_by, role` (`shared_lists.sql`; unique `(list_id, user_id)` — membership of a **shared list**. Owner is implicit via `Lists.user_id`; a member = owner OR a row here, per `is_list_member()`. Any member can edit, per the `*_member` RLS policies) |
| `list_cover_defaults` | `list_name, cover` (branded cover data URLs for the auto-managed **Recs** / **Bucket List**; one row per name. Source of truth read by the signup trigger + the `lists_branded_covers.sql` backfill) |
| `swipes` | `id, created_at, user_id, tmdb_id, direction ('left'|'right'), genres` (`swipes.sql` + `genres text[]` from `swipe_appeal.sql`; unique `(user_id, tmdb_id)` — one per movie the user swiped in the **Discover** deck. Keyed by `tmdb_id` (not `Movies.id`) since most candidates aren't in the catalog; right = added to Bucket List, left = 30-day soft-skip. `genres` = the card's genre names at swipe time, used for Phase 3 swipe-appeal. RLS: own rows) |
| `Recommendations` | `id, created_at, from_user_id, to_user_id, movie_id` (unique `(from_user_id, to_user_id, movie_id)`; logs who recommended what to whom — blocks re-recommends + drives "Recommended Again") |
| `Achievements` | `id, created_at, name, description, icon_url, tier, points, is_active, type, rule, family` (`rule` jsonb drives the data-driven award engine; `icon_url` may be inline `<svg>`/data-URL/http — SVGs render as a tier-framed medallion so one icon covers all tiers; `family` groups sibling tiers to share an icon; `family` added by `achievements_overhaul.sql`) |
| `User Achievements` | `id, user_id, achievement_id, earned_at` |
| `User Tiers` | `id, created_at, tier_icon_url, name, points_needed` |
| `Genres` | `id, name` |
| `Themes` | `id, created_at, name, colors` |
| `Logos` | `id, created_at, theme_id, url` |
| `Background Images` | `id, created_at, page, url, name, theme_id` |
| `Loading Images` | `id, image_url, last_used` |
| `Help Pop-ups` | `user_id, "AI Picks Help", "New Feature News"` |
| `Feature Requests` | `user_id, created_at, feature` |
| `Settings` | `id, created_at, allow_signups` |
| `Taste Profiles` | `user_id, computed_at, mean_overall, std_overall, like_threshold, runtime_bins_json, decade_bins_json, people_affinity_json, subrating_weights_json, imdb_delta, median_overall, genre_affinity_json, keyword_affinity_json, swipe_genre_affinity_json, taste_blurb` (`taste_blurb` (added by `taste_blurb_column.sql`, not in the CSV export) = a cached AI-written one-line taste snippet shown on the fun Account page — generated by `taste_profile_edge.js` each recompute, best-effort (omitted from the upsert on failure so the old value persists); `genre_affinity_json` added by `taste_genre_affinity.sql`, `keyword_affinity_json` by `movie_keywords.sql`, `swipe_genre_affinity_json` by `swipe_appeal.sql` (per-genre right-swipe-rate lift from the Discover deck: `{ "action": { name, right, total, rate, lift, aff } }`, `aff`=`lift*100`; a WEAK signal blended low into `predictTasteScore`), none in the CSV export — per-genre / per-keyword affinity keyed by NAME: `{ "Science Fiction": { avg, count, shrunk, aff } }` where `aff` = `shrunk - mean_overall` = how far above/below baseline the user rates that genre/theme. `keyword_affinity_json` is theme-level taste (TMDB keywords like "heist"/"dystopia"), capped to the ~60 strongest `|aff|` keywords with ≥3 ratings each. `people_affinity_json` and the runtime/decade bins now also carry `shrunk`+`aff`; people require ≥2 rated movies and rank by `aff`, not raw avg. All use **Bayesian shrinkage toward the user's mean** (pseudo-count `SHRINK_K=5`) so a single rating can't create a fake favorite) |

### How to read denormalized movie data
The front end usually reads **genre/director/platforms together via SQL views**
(`library_views.sql` → `user_library_items_v2`, `user_movie_latest_watch`), not by
joining the base tables itself. Server-side (Edge Function) you must join the base
tables manually: genre = `Movie Genres`→`Genres`; director = `Movie Crew`
(`job='Director'`)→`People`.

## Editing conventions

- Match the existing style: top-level code in each file is indented **8 spaces**
  (carried over from when it was nested inside `<script>` in `index.html`). Keep
  new code consistent with the surrounding lines rather than re-indenting.
- Inline `onclick`/`oninput` handlers in `index.html` markup require their target
  functions to remain global — don't rename a function without updating its
  callers in `02-router.js` templates and `index.html`.
- When adding a new feature file, give it the next number, add its `<script src>`
  tag to `index.html` in the correct order, and update this map.
- **Default to SOLID / OPAQUE surfaces — do NOT make things see-through.** Cards,
  modal/sheet bodies, popovers, dropdowns, buttons, and the backs of flip cards must
  use solid backgrounds (e.g. `#16161a`/`#202024`), never a low-alpha `rgba(...)` or
  the translucent `--glass-bg`, unless the element is *specifically* a glass overlay
  by design. A see-through panel that shows the content behind it reads as a bug.
  (This burned the Discover card back, which had to be switched off `--glass-bg`; the
  button classes were likewise made opaque — see the "No see-through buttons" note.)
